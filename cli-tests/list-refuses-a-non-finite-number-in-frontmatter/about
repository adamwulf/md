Guards the clean per-file refusal from `md list --format json` when any one
file in the directory holds .nan. This path once aborted with SIGABRT.

This pairs with format-refuses-a-non-finite-number-in-frontmatter. Both remain
worth keeping: they are one defect, but two code paths reach Foundation's
serializer.
