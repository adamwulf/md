Eight paragraphs, each one holding a marker that a backslash makes inert. Every
one of them must still be a paragraph after the write. Four of them would change
the type of the block: a heading, a list, a list, and a block quote.
