A nested array projected with `frontmatter --key` keeps each top-level item on
one line and serializes the nested collection in the fence format. Recursively
flattening the value makes a matrix indistinguishable from a flat list.
