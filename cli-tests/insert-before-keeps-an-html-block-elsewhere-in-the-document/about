md counts two blocks here, the heading and the tail paragraph,
because it does not model an html block. Whatever it counts, the
html must survive the edit. insert-before splices into the source,
so it does.
