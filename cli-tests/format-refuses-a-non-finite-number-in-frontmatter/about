Guards the clean refusal from `md format --frontmatter json` on a document
whose frontmatter holds .nan. This path once aborted with SIGABRT.

This case exists in the CLI suite rather than the Swift suite because a
SIGABRT cannot be asserted from inside the process that dies. The CLI case
therefore pins the exit code and exact diagnostic outside the process.
